#
# spec file for package loopidity
#
# Copyright (c) 2012-2107 bill-auger bill-auger@programmer.net
#
# All modifications and additions to the file contributed by third parties
# remain the property of their copyright owners, unless otherwise agreed
# upon. The license for this file, and modifications and additions to the
# file, is the same license as for the pristine package itself (unless the
# license for the pristine package is not an Open Source License, in which
# case the license is the MIT License). An "Open Source License" is a
# license that conforms to the Open Source Definition (Version 1.9)
# published by the Open Source Initiative.

# Please submit bugfixes or comments via http://bugs.opensuse.org/


Name:          loopidity
Version:       0.00.000
Release:       1%{?dist}
Summary:       Multi-track multi-channel looping audio recorder for live hands-free use
License:       GPL-3.0
URL:           https://github.com/bill-auger/%{name}/
Source0:       https://github.com/bill-auger/%{name}/archive/v%{version}.tar.gz
BuildRequires: gcc-c++ automake
Requires:      SDL_gfx SDL_ttf
%if 0%{?suse_version}
BuildRequires: autoconf-archive jack-devel libX11-devel SDL_gfx-devel SDL_ttf-devel update-desktop-files
Requires:      jack
%endif
%if 0%{?fedora_version}
BuildRequires: jack-audio-connection-kit-devel libX11-devel SDL_gfx-devel SDL_ttf-devel
Requires:      jack-audio-connection-kit
%endif
%if 0%{?mageia}
BuildRequires: autoconf-archive libjack-devel libSDL_gfx-devel libSDL_ttf-devel libx11-devel
Requires:      jack
%endif
%description
  Loopidity is a native GNU/Linux and Windows application built with
  the SDL framework and using the JACK Connection Kit for audio I/O.
  Loopidity is designed so that the most useful functions can be controlled
  using as few buttons as possible (a small number of foot pedals for example).


%prep
%autosetup

%build
autoreconf -fi
./configure --prefix=/usr
make %{?_smp_mflags}

%install
make DESTDIR="%{buildroot}/" install
rm -rf %{buildroot}/usr/share/doc/%{name}

%files
%defattr(-,root,root)
%license COPYING assets/COPYING.fonts
%doc LICENSES assets/LICENSES.assets
%{_bindir}/%{name}
%{_datadir}/%{name}
%if 0%{?mageia}
%{_mandir}/man1/%{name}.1.xz
%else
%{_datadir}/%{name}/histogram_gradient.bmp
%{_datadir}/%{name}/loop_gradient.argb.bmp
%{_datadir}/%{name}/Purisa.ttf
%{_datadir}/%{name}/scope_gradient.bmp
%{_mandir}/man1/%{name}.1.gz
%endif

%post
%if ! 0%{?suse_version}
  xdg-desktop-menu forceupdate
%endif

%postun
%if ! 0%{?suse_version}
  xdg-desktop-menu forceupdate
%endif

